<h2>Neuen Mitarbeiter anlegen:</h2>

<%= form_for(@employee) do |f| %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>